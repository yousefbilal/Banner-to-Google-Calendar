{
  "dependencies": {
    "ics": "^3.7.6",
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.12.0",
    "parcel": "^2.12.0"
  },
  "scripts": {
    "build": "rm -rf ./dist && parcel build manifest.json --config @parcel/config-webextension",
    "build:test": "rm -rf ./dist && parcel build manifest.json --config @parcel/config-webextension --no-optimize"
  }
}